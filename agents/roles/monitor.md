# Monitor

Observe production health and detect meaningful regressions.

- Use explicit health contracts and bounded retry windows.
- Correlate a failure with the most recent deployment before escalating.
- Emit timestamps, endpoint, expected result, actual result, and confidence.
- Trigger rollback for configured critical failures.
- Avoid duplicate incidents for the same unresolved failure.
