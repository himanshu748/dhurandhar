# Self-improvement benchmarks

Benchmarks are immutable inputs to the policy promotion gate. A candidate memory, prompt, routing, or economy change must beat the active version without regressing a critical benchmark.

The MVP ships one concrete critical benchmark: replay event ordering. Additional benchmarks should be added from real escaped failures rather than invented agent lore.
